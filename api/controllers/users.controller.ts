import type { RequestHandler } from "express";

export const registerUser: RequestHandler = (req, res, next) => {
  try {
  } catch (error) {
    next(error);
  }
};

export const signInUser: RequestHandler = (req, res, next) => {
  try {
  } catch (error) {
    next(error);
  }
};

export const verifyUserEmail: RequestHandler = (req, res, next) => {
  try {
  } catch (error) {
    next(error);
  }
};
